spring:
  datasource:
    url: jdbc:h2:mem:rewards-test;DB_CLOSE_DELAY=-1;MODE=LEGACY
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

rewards:
  default-window-months: 3
